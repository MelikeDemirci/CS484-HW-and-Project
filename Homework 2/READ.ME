- In order to stitch images run main.py script
- While script runs it will ask for the txt filename
This txt file should be in the same root with the main script
- All the input images should be in the data folder
- Resulting image will be saved as "result.jpg" inside the result folder
- Feature point matches will be saved inside matches folder

For the requirements check requirements.txt
All dependencies can be installed by running 
pip install -r requirements.txt in the terminal